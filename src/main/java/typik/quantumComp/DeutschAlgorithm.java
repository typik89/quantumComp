package typik.quantumComp;

import java.math.BigDecimal;
import java.util.function.Function;

public class DeutschAlgorithm {
	
	public static boolean isFunctionConstant(Function<BigDecimal, BigDecimal> func){
		Vector vector = new Vector( new ComplexNumber[]{ 
				new ComplexNumber( 0 , 0 ),
				new ComplexNumber( 1 , 0 )
		} );
		Vector r = new H( 2 ).multiply( )
		
		return false;
		
	}
}
